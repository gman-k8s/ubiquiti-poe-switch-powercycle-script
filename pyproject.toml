[project]
name = "unifi-poe-powercycle"
version = "0.1.0"
description = "Power-cycle a UniFi PoE switch port when the device behind it stops answering pings."
requires-python = ">=3.9"
dependencies = [
    "requests>=2.31,<3",
]

[tool.uv]
package = false
